# AGENTS

Keep the project focused on RGB static image enhancement. Do not add video, event camera, audio, frame sequence, ConvGRU, or temporal features.

Model outputs must be honest. If official source or pretrained weights are missing, mark the model unavailable and return a clear reason.
